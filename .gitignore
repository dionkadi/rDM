# Rust build artifacts (workspace + src-tauri)
/target
target/

# Node dependencies
node_modules/

# Frontend production build (regenerated by `npm run build`)
dist/

# Plannotator / agent harness artifacts
.pi/

# Logs
*.log

# OS / editor
.DS_Store
Thumbs.db

# Local-only artifacts (Tauri/webview crash dumps and the
# README-screenshot scratch dir; AGENTS.md documents both as
# gitignored so the next agent doesn't try to commit them).
coredump/
images/
